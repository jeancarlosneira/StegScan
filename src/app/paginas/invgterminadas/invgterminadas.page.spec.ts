import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InvgterminadasPage } from './invgterminadas.page';

describe('InvgterminadasPage', () => {
  let component: InvgterminadasPage;
  let fixture: ComponentFixture<InvgterminadasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InvgterminadasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
