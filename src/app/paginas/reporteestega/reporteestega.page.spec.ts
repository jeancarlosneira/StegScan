import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReporteestegaPage } from './reporteestega.page';

describe('ReporteestegaPage', () => {
  let component: ReporteestegaPage;
  let fixture: ComponentFixture<ReporteestegaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReporteestegaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
